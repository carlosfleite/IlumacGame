# Ambiente virtual
.venv/

# Bytecode Python
__pycache__/
*.py[cod]

# Banco de dados local do totem — contém dados pessoais dos participantes
# (LGPD) e é a fonte da verdade durante o evento. NUNCA versionar: um
# `git pull` no totem sobrescreveria os cadastros reais da feira.
*.db
*.db-wal
*.db-shm

# Logs do totem e do watchdog (gerados em runtime)
logs/

# Sinalizador de parada limpa do watchdog
PARAR.flag

# Exportações geradas pelo painel de dados
export/
*.csv

# Credenciais do painel admin (hash da senha + chave de sessão) — geradas
# sozinhas no primeiro boot. NUNCA versionar mesmo sendo só um hash: a
# chave de sessão em texto puro permitiria forjar um cookie autenticado.
config/admin.json

# Backups manuais do banco (gerados durante manutenção pontual)
quiz.db.backup_*

# Sistema operacional
Thumbs.db
desktop.ini
